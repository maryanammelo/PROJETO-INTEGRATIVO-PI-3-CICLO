#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
    setlocale(LC_ALL, "Portuguese");

    int c1=0, c2=0, c3=0, c4=0, c5=0, p11=0, p12=0, p13=0, p14=0, p15=0, p21=0, p22=0, p23=0, p24=0, p31=0, p32=0, p33=0, p34=0, p41=0, p42=0, p43=0, p44=0, p51=0, p52=0, p53=0, p54=0;
    char resp;

    while (1){
        printf("\n1. Análise e especificação de requisitos:\n\n");

        if(p11==1){
            printf("Identificação de stakeholders finalizada.\n");
        }
        else{
            printf("1.1. A identificação de stakeholders já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c1 = c1 + 10;
                p11 = 1;
            }
        }

        if(p12==1){
            printf("Levantamento de requisitos funcionais e não funcionais finalizado.\n");
        }
        else{
            printf("1.2. O Levantamento de requisitos funcionais e não funcionais já foi concluído? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c1 = c1 + 35;
                p12 = 1;
            }
        }

        if(p13==1){
            printf("Definição da prioridade dos requisitos finalizada.\n");
        }
        else{
            printf("1.3. A definição da prioridade dos requisitos já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c1 = c1 + 20;
                p13 = 1;
            }
        }

        if(p14==1){
            printf("Documentação dos requisitos finalizada.\n");
        }
        else{
            printf("1.4. A documentação dos requisitos já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c1 = c1 + 20;
                p14 = 1;
            }
        }

        if(p15==1){
            printf("Validação de requisitos com os stakeholders finalizada.\n");
        }
        else{
            printf("1.5. A validação de requisitos com os stakeholders já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c1 = c1 + 15;
                p15 = 1;
            }
        }

        printf("\n2. Elaboração do projeto:\n\n");

        if(p21==1){
            printf("Definição da arquitetura do sistema finalizada.\n");
        }
        else{
            printf("2.1. A definição da arquitetura do sistema já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c2 = c2 + 25;
                p21 = 1;
            }
        }

        if(p22==1){
            printf("Criação de diagramas de modelagem (BPMN, UML) finalizada.\n");
        }
        else{
            printf("2.2. A criação de diagramas de modelagem (BPMN, UML) já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c2 = c2 + 30;
                p22 = 1;
            }
        }

        if(p23==1){
            printf("Design do banco de dados finalizado.\n");
        }
        else{
            printf("2.3. O design do banco de dados já foi concluído? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c2 = c2 + 20;
                p23 = 1;
            }
        }

        if(p24==1){
            printf("Planejamento de interface e experiência do usuário (UX/UI) finalizado.\n");
        }
        else{
            printf("2.4. O planejamento de interface e experiência do usuário (UX/UI) já foi concluído? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c2 = c2 + 25;
                p24 = 1;
            }
        }

        printf("\n3. Codificação:\n\n");

        if(p31==1){
            printf("Configuração do ambiente de desenvolvimento finalizada.\n");
        }
        else{
            printf("3.1. A configuração do ambiente de desenvolvimento já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c3 = c3 + 10;
                p31 = 1;
            }
        }

        if(p32==1){
            printf("Implementação dos módulos principais finalizada.\n");
        }
        else{
            printf("3.2. A implementação dos módulos principais já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c3 = c3 + 50;
                p32 = 1;
            }
        }

        if(p33==1){
            printf("Integração de funcionalidades finalizada.\n");
        }
        else{
            printf("3.3. A integração de funcionalidades já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c3 = c3 + 30;
                p33 = 1;
            }
        }

        if(p34==1){
            printf("Documentação do código finalizada.\n");
        }
        else{
            printf("3.4. A documentação do código já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c3 = c3 + 10;
                p34 = 1;
            }
        }

        printf("\n4. Fase de testes:\n\n");

        if(p41==1){
            printf("Testes unitários finalizados.\n");
        }
        else{
            printf("4.1. Os testes unitários já foram concluídos? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c4 = c4 + 25;
                p41 = 1;
            }
        }

        if(p42==1){
            printf("Testes de integração finalizados.\n");
        }
        else{
            printf("4.2. Os testes de integração já foram concluídos? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c4 = c4 + 25;
                p42 = 1;
            }
        }

        if(p43==1){
            printf("Testes de sistema e aceitação finalizados.\n");
        }
        else{
            printf("4.3. Os testes de sistema e aceitação já foram concluídos? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c4 = c4 + 30;
                p43 = 1;
            }
        }

        if(p44==1){
            printf("Correções de bugs finalizadas.\n");
        }
        else{
            printf("4.4. As correções de bugs já foram concluídas? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c4 = c4 + 20;
                p44 = 1;
            }
        }

        printf("\n5. Implantação:\n\n");

        if(p51==1){
            printf("Configuração do ambiente de produção finalizada.\n");
        }
        else{
            printf("5.1. A configuração do ambiente de produção já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c5 = c5 + 30;
                p51 = 1;
            }
        }

        if(p52==1){
            printf("Implantação da solução finalizada.\n");
        }
        else{
            printf("5.2. A implantação da solução já foi concluída? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c5 = c5 + 40;
                p52 = 1;
            }
        }

        if(p53==1){
            printf("Treinamento de usuários finalizado.\n");
        }
        else{
            printf("5.3. O treinamento de usuários já foi concluído? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c5 = c5 + 20;
                p53 = 1;
            }
        }

        if(p54==1){
            printf("Suporte inicial pós-implantação finalizado.\n");
        }
        else{
            printf("5.4. O suporte inicial pós-implantação já foi concluído? Digite 's' para sim, e 'n' para não: ");
            scanf(" %c", &resp);
            if(resp == 's'){
                c5 = c5 + 10;
                p54 = 1;
            }
        }


        printf("\nSumário das etapas do projeto:\n");
        printf("Análise e especificação de requisitos: %d%%\n", c1);
        printf("Elaboração do projeto: %d%%\n", c2);
        printf("Codificação: %d%%\n", c3);
        printf("Fase de testes: %d%%\n", c4);
        printf("Implantação: %d%%\n\n", c5);

        if (c1 == 100 && c2 == 100 && c3 == 100 && c4 == 100 && c5 == 100){
            printf("O projeto foi finalizado.");
            return 0;
        }

        printf("Atualização dos estágios de cada etapa:\n");
    }
}
